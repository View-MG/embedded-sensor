{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "lint": "echo \"Skipping lint for Firebase Functions\"",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "dotenv": "^17.2.3",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.0.0",
    "mongodb": "^6.21.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "typescript": "^5.0.0"
  },
  "private": true
}
